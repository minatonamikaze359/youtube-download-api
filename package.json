{
  "name": "yt-downloader-api",
  "version": "1.0.0",
  "description": "YouTube downloader API on Netlify",
  "main": "netlify/functions/api.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "serverless-http": "^3.2.0",
    "ytdl-core": "^4.11.5"
  }
}
